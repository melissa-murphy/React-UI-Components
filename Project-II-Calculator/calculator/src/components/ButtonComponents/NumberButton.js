import React from 'react';
import './Buttons.scss';

const NumberButton = () => {
    return (
        <div>
            <button>Number Button</button>
        </div>
    )
};

export default NumberButton;
