import React from 'react';
import './Buttons.scss';

const ActionButton = () => {
    return (
        <div>
            <button>Action button</button>
        </div>
    )
};

export default ActionButton;
